node_modules
.svelte-kit
build
package-lock.json

# Generated by the data pipeline — never hand-edit or reformat (MASTER.md §4.7, §7)
src/lib/data/schedule.json
static/posters
.build

# Golden corpus — a formatting change here reads as a source change (MASTER.md §4.6)
test/golden

# Specification prose is authored, not generated. Prettier would repad every table
# and reflow the hand-aligned token columns in DESIGN.md's code fences, producing a
# large diff in the two documents that are the source of truth. Formatted by hand.
MASTER.md
DESIGN.md
tasks/
